import {
  Events as DatePickerEvents,
  DatePickerConfig
} from './DatePicker/index'

export const DatePickersEvents = {
  [DatePickerConfig.key]: DatePickerEvents,
}

export default [DatePickerConfig]
